import { Canvas, Meta, Story } from '@storybook/addon-docs';
import FormInput from './FormInput.vue';
import FormRoot from '../FormRoot/FormRoot.vue';

<Meta title="MDX/FormInput" component={FormInput} />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { FormInput, FormRoot },
  template: `
    <div class="flex flex-col gap-1">
      <label for="input">Input</label>
      <FormInput v-bind="$props" />
    </div>
  `,
});

# FormInput

With `MDX`, we can define a story for `FormInput` right in the middle of our
Markdown documentation.

<Canvas>
  <Story 
    name="FormInput"
    args={{
      id: 'input',
      placeholder: 'Type here',
      float_label: false,
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>